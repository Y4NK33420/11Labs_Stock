# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=3000
WEBHOOK_URL=your-deployment-url # Required for external service callbacks

# Alpha Vantage Configuration
ALPHA_VANTAGE_API_KEY=your_alphavantage_api_key # Required for stock market data

# Search Configuration
SERPAPI_API_KEY=your_serpapi_api_key # Required for search functionality

# Database Configuration (Optional)
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
SUPABASE_PROJECT_ID=your_project_id

# Security Configuration (Optional)
JWT_SECRET=your_jwt_secret
API_KEY=your_api_key # For API authentication

# Optional Configurations
DEBUG=True
ENVIRONMENT=development # or production
CACHE_TIMEOUT=3600 # Cache timeout in seconds
MAX_REQUESTS_PER_MINUTE=100 # Rate limiting